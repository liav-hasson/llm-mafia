# Very basic cluster bootstrap
# Networking is done with Docker bridge network driver

kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes: 
  - role: control-plane
  - role: worker
  - role: worker
